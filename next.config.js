/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
          'i.dummyjson.com',
          'lh3.googleusercontent.com'
        ]
      } 
}

module.exports = nextConfig
